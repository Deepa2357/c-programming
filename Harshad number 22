#include<stdio.h>
#include<math.h>
int main()
{
    int n,sum=0;
    scanf("%d",&n);
    int t=n;
    while(n>0){
        sum+=n%10;
         n=n/10;
    }
    if(t%sum==0)
       printf("Harshad Number"); 
    else
         printf("Not Harshad Number");
}

